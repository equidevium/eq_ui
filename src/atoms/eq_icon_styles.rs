//! Style constants for EqIcon.

/// Icon wrapper — centres content and prevents shrinking
pub const WRAPPER: &str = "inline-flex items-center justify-center shrink-0";

/// Small icon (16 × 16)
pub const SM: &str = "size-4";
/// Medium icon (20 × 20) — default
pub const MD: &str = "size-5";
/// Large icon (24 × 24)
pub const LG: &str = "size-6";

/// Muted / secondary colour
pub const MUTED: &str = "text-[var(--color-label-secondary)]";
/// Default / primary colour
pub const DEFAULT: &str = "text-[var(--color-label-primary)]";
